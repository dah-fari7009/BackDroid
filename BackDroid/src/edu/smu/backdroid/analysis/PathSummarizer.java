/**
 * Class to build interactivity summaries
 * Act 1 --> Act 2, with --> describing call sequence and associated patterns/sequences
 */
public class PathSummarizer{
    //private static Map<String, Set<PATTERN>> summaries;
    

    public void addEntry(BDG bdg){
        //For each init node in the graph
        //Get patterns using PatternDetector
        //Get (all) target intent classes
        //Add pair initNode.get entrypoint, destination, where??
    }
}